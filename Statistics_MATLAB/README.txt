The statistics can be calculated with ACC_PSNR_SNR_All.m file. The user needs to give image paths, set the threshold value, and give the file name. All statistics will be written into a txt file.

histograms.m generates the histogram for a given image for all configurations.

The neighbourhood configurations can be generated with scripts in census_matrixes file. 